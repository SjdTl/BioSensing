//Import serial library

//Create an instance of the serial interface

/**
 * @brief Function that is executed once, when the Arduino boots
 * 
 */
void setup()
{

  //Initiate serial connection

}

/**
 * @brief Function that is executed consecutively in a loop after setup has been run
 * 
 */
void loop()
{

  // Sample analog pins at >= 100 Hz

  // Transfer sampled data using serial
  // Format [Timestamp, ECG data, GSR data]
 
}
